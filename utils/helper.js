/*teachapp.filter('roleFilter', function () {
  return function (items, role) {
    var filtered = [];
    for (var i = 0; i < items.length; i++) {
      var item = items[i];
      if ( role == item.user.role) {
        filtered.push(item);
      }
    }
    return filtered;
  };
});*/
